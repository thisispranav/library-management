CREATE DATABASE LMS;

USE LMS

CREATE TABLE BOOKs(
BNO INT,
BN VARCHAR(50),
FARD CHAR(1),
AN VARCHAR(50),
PRIMARY KEY (BNO));

CREATE TABLE MEMBERS(
RNO INT,
NAME VARCHAR(50),
NUM BIGINT,
ADR VARCHAR(150),
BIS INT,
PRIMARY KEY (RNO));
